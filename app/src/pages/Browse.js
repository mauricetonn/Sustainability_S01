import React from "react";
import Header from "../Header.js"

// wird bei "/browse" aufgerufen
// Hier soll später das Scrollen durch die Anzeigen erfolgen

const Browse = () => {
    return (
      <div>
            <Header />
            <h1>Browse</h1>
      </div>
    );
  };
  
  export default Browse;